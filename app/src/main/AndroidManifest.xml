<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.classroom.app1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".UI.Seller.SellerHome"
            android:label="@string/title_activity_seller_home"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".UI.Seller.SellersOrders">

        </activity>
        <activity android:name=".UI.Seller.AddProduct" />
        <activity android:name=".UI.UsersOrders" />
        <activity android:name=".UI.Cart" />
        <activity android:name=".UI.Register" />
        <activity android:name=".Helpers.BaseActivity" />
        <activity android:name=".UI.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.ProductActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UI.Profil"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".UI.ProductsListActivity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".UI.HomeActivity"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>